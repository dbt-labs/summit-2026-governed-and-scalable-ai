version: 2

# [answer key — disabled] Intermediate model for the procurement lab.

models:
  - name: int_potion_supply_cost
    description: "[answer key] Cost to brew one unit of each potion (see model header for the unit assumption)."
    columns:
      - name: potion_sku
        description: Primary key — one row per potion with a recipe.
        data_tests: [unique, not_null]
