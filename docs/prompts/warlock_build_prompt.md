# Prompt: Building the ungoverned Warlock models

Use this prompt to build the ungoverned Alembic slice.

```text
Build models for the alembic_ops source in models/warlock; give each model the suffix '__warlock'
```